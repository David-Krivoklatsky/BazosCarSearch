# OpenRouter LLM configuration for deal evaluation (Phase 3).
# api_key comes from OPENROUTER_API_KEY / BAZCAR_OPENROUTER_API_KEY in .env.

base_url: "https://openrouter.ai/api/v1"
model: "openai/gpt-4o-mini"
temperature: 0.2
max_tokens: 400